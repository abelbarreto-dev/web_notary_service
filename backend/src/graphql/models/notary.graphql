enum NotaryStatus {
    PENDING,
    IN_PROGRESS,
    COMPLETED
}

enum NotaryType {
    BIRTH_CERTIFICATE,
    SIGNATURE_AUTHENTICATION,
    DOCUMENT_AUTHENTICATION,
    DEED,
    OTHER
}

input NotaryInput {
    id: ID
    applicant: String!
    cpf: String!
    description: String!
    requestDate: DateTime!
    remarks: String
    notaryStatus: NotaryStatus!
    notaryType: NotaryType!
}

input NotaryId {
    id: String!
}

type Notary {
    id: ID!
    applicant: String!
    cpf: String!
    description: String!
    requestDate: DateTime!
    remarks: String
    notaryStatus: NotaryStatus!
    notaryType: NotaryType!
    createdAt: DateTime!
    updatedAt: DateTime
}

extend type Query {
    findAllNotaries: [Notary!]!
}

extend type Mutation {
    createNotary(notary: NotaryInput): User,
    updateNotary(notary: NotaryInput): User,
    deleteNotary(notary: NotaryId): User,
}
